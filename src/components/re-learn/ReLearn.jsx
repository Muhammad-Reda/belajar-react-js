import { ThreadProvider } from "./Context";
import Content from "./Content";

const Relearn = () => {
    return (
        <ThreadProvider>
            <Content />
        </ThreadProvider>
    );
};

export default Relearn;
